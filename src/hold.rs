use std::io::Result;

use crate::mino::MinoType;

const HOLD_SLOT_HEIGHT: u16 = 4;
const HOLD_SLOT_WIDTH: u16 = 6;

pub fn display_hold(column: u16, row: u16, held_mino: MinoType) -> Result<()> {
    

    Ok(())
}
